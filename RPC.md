#RPC Tutorial
É como o exemplo simples, só que adicionamos um callback, ou sejá o publisher envia a mensagem para uma queue esta é consumida por um consumer,
assim que consumida ela é processada e publicada um response em uma queue de callback,e por fim, esta mensagem é consumida pelo publisher.

